export interface forecast
{
    CloudsCounter: Number,
    RainCounter: Number,
    DrizzleCounter: Number,
    MistCounter: Number,
    StormCounter: Number,
    ThunderstormCounter: Number,
    SunnyCounter: Number,
    ClearCounter: Number,
    HazeCounter: Number,
    FogCounter: Number,
    SmokeCounter: Number,
    SnowCounter: Number,
    avgTemp: Number,
    counter: Number,
    predictedState: string,
    stateImg: string
}